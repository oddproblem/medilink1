name: medilink1
description: SwiftMediLink - Doctor & Patient multi-login demo UI
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  fl_chart: ^0.67.0
  http: ^1.1.0
  shared_preferences: ^2.2.2
  webview_flutter: ^4.0.7
  webview_flutter_android: ^3.9.4
  url_launcher: ^6.1.10
  intl: ^0.19.0
  provider: ^6.1.2
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - translations/
